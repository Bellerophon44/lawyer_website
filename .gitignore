# Claude Code — local per-machine settings (permissions allowlist)
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Node (si on en arrive là plus tard)
node_modules/
.cache/

# Build de production (regenere par tools/build.mjs)
dist/

# Identifiants de deploiement — ne jamais committer
.env
deploy/.env
